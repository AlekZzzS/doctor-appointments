Проект развернут в докере, скачиваем его, и используем команду `docker-compose up --build`.

После запуска контейнеров, прописываем `npm run fixtures`, добавит парочку записей.

Использовал:
- Node.js
- Express
- PostgreSql
- Redis
- Docker
